/**
 * Exercice 1.1 : a
 */

// function setup() {
//     // Crée un canvas de la taille de la fenêtre
//     createCanvas(windowWidth, windowHeight);
//     // Met la valeur de fond à 100
//     background(100);
//  }
 
 
//  function draw() {
//     // Crée un cercle de la taille 20 à la position de la souris
//     circle(mouseX, mouseY, 20);
//  }
 

/**
 * Exercice 1.1 : b
**/

// function setup() {
//         // Crée un canvas de la taille de la fenêtre
//         createCanvas(400, 400);
//         // Met la valeur de fond à 100
//         background(0,0,255);
//      }
     
     
//      function draw() {
//         // Crée un cercle de la taille 20 à la position de la souris
//         circle(mouseX, mouseY, random(10, 100));
//     }



/**
 * Exercice 1.1 : c
 * Réponse : le cercle est toujours visible, le background n'est pas visible et la canva ne s'applique pas à la fenêtre
 */

// function setup() {
//     // Met la valeur de fond à 100
//     background(100);

//     // Crée un canvas de la taille de la fenêtre
//     createCanvas(windowWidth, windowHeight);
    
//  }
 
 
//  function draw() {
//     // Crée un cercle de la taille 20 à la position de la souris
//     circle(mouseX, mouseY, 20);
//  }

/**
 * Exercice 1.1 : d
 * 
 */

function setup() {
    

    // Crée un canvas de la taille de la fenêtre
    createCanvas(windowWidth, windowHeight);


    // Met la valeur de fond à 100
    background(125,125,0);
    
 }
 
 
 function draw() {
    // Crée un cercle de la taille 20 à la position de la souris
    circle(windowWidth/2, windowHeight/2, 100);
 }